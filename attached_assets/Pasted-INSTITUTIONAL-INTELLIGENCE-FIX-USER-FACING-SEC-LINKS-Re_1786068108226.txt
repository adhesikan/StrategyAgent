INSTITUTIONAL INTELLIGENCE — FIX USER-FACING SEC LINKS

Repository:
VCP Trader AI / StrategyAgent

PRODUCTION DEFECT

The Institutional Intelligence unavailable state contains a link labeled:

“Search 13F filings on SEC EDGAR”

The link currently opens a machine-readable SEC search endpoint similar to:

https://efts.sec.gov/LATEST/search-index?... 

This returns raw JSON in the browser and is not an appropriate user-facing
destination.

This is a focused link and UX correction only.

Do not modify 13F ingestion, database schema, APIs, institutional calculations,
Opportunity Engine, MCP, scanner, broker logic, or other research tabs.

TASKS

1. Find the Institutional Intelligence unavailable/disabled-state component and
   identify the current SEC link.

2. Remove the user-facing link to the raw EFTS/search-index JSON endpoint.

3. Replace it with a human-readable link:

Primary label:
“Search 13F filings on SEC EDGAR”

Destination:
the official SEC EDGAR Full Text Search interface, not the raw API.

The destination should allow the user to search public filings interactively.

4. Add an optional second link:

Label:
“View SEC Form 13F data sets”

Destination:
the official SEC Form 13F Data Sets page.

5. Open external SEC links in a new tab using:

target="_blank"
rel="noopener noreferrer"

6. Keep raw SEC endpoints available only for backend/internal ingestion code.

Do not expose these machine endpoints as clickable user links:

- efts.sec.gov/LATEST/search-index
- data.sec.gov JSON endpoints
- raw filing XML
- bulk ZIP files

unless explicitly presented as developer/download resources.

7. Refine the unavailable state copy to:

“13F institutional data is not available for COST because Institutional
Intelligence is not enabled on this deployment.”

Supporting copy:

“SEC Form 13F filings are public, quarterly disclosures from qualifying
institutional investment managers. They are delayed and do not represent
real-time or complete institutional activity.”

Actions:

[Search 13F filings on SEC EDGAR]
[View SEC Form 13F data sets]

8. Once Institutional Intelligence has valid internal data, the main user workflow
   must remain inside VCP Trader.

Individual source links may later open the relevant SEC filing detail page using a
safe accession-number link—not a raw search response.

9. Add tests confirming:

- primary link does not contain “search-index”
- primary link does not use efts.sec.gov
- primary link points to the human-readable SEC EDGAR search experience
- dataset link points to the official Form 13F datasets page
- links use noopener and noreferrer
- unavailable-state disclaimer remains visible
- no raw JSON endpoint is exposed in rendered UI

10. Run client tests, server tests, typecheck and build.

RETURN

- exact file changed
- old URL
- new destinations
- tests added
- totals
- build result
- final GO / NO_GO

Do not commit, push or deploy automatically.