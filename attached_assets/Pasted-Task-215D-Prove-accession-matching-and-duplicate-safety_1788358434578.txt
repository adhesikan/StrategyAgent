Task #215D — Prove accession matching and duplicate safety before historical APPLY

Continue from commit:

a8e4c413582ae055c8510015058c2c4eaa3437cc

The production dry-run successfully completed all seven quarters with bounded memory.

Results:

quarters: 7
existingFilings: 218
sourceFilings: 59,375
filingsToInsert: 59,375
estimatedHoldingRowsToProcess: 22,936,779
heap peak: approximately 166 MB

The remaining concern is:

filingsAlreadyPresent = 0

for every quarter despite existing production filings being present.

DO NOT RUN APPLY.

OBJECTIVE

Determine whether zero overlap is correct or whether accession comparison /
normalization is incorrect.

Trace only the canonical filing identity used by:

SEC bulk source
→ parser
→ institutional_13f_filings persistence
→ historical dry-run comparison
→ idempotent INSERT/UPSERT path

PROVE:

1. What exact field uniquely identifies a filing in production.
2. What exact accession representation is produced by the SEC bulk parser.
3. What exact accession representation exists in institutional_13f_filings.
4. Whether differences such as:
   - dashes
   - leading/trailing whitespace
   - case
   - SEC prefix formatting
   - numeric/string conversion
   - amendments
   - manager identifier
   - report-period vs filing-period
   cause false non-matches.

Run ONE bounded read-only reconciliation over the seven-quarter range.

Return counts only:

existingFilings
existingAccessions
sourceFilings
sourceAccessions
exactOverlap
normalizedOverlap
existingOnly
sourceOnly
duplicateExistingCanonicalAccessions
duplicateSourceCanonicalAccessions

Also report at most 10 sanitized accession-format examples if needed to prove
a normalization discrepancy.

If matching is wrong:

Fix the comparison and persistence identity GENERICALLY.

The same canonical accession normalization must be used by:

- dry-run comparison
- ingestion
- resumability checks
- idempotency checks
- amendment handling

Do not create a second identity scheme.

DATABASE SAFETY

Before allowing APPLY, prove that rerunning the same source filing cannot
create a second filing or duplicate its holdings.

Verify the relevant database uniqueness/upsert protection.

Do not add destructive deduplication.
Do not modify existing production data during this task.

SCALE CHECK

Also report projected write scale from the current dry-run:

filingsToInsert
holdingRowsToProcess

and confirm that persistence is bounded/batched and does not require a
single transaction containing an entire 3+ million-row quarter.

Do not estimate or redesign infrastructure unless an actual blocker exists.

TESTS

Add/fix tests only if needed for:

- dashed vs normalized SEC accession
- exact rerun idempotency
- partial accession resume
- amendment identity
- duplicate filing prevention
- duplicate holding prevention

Do not reopen:
- OpenFIGI
- mapping repair
- asset types
- canonical institutional identity
- Stock View
- trend/runtime work

RETURN ONLY:

ACCESSION MATCHING FINDING

PRODUCTION OVERLAP

DUPLICATE SAFETY

FIX if required

SCALE SAFETY

FILES CHANGED

TEST RESULTS

COMMIT SHA

PRODUCTION BLOCKER YES/NO

If and only if PRODUCTION BLOCKER is NO, provide exactly ONE guarded
production APPLY command for the appropriate historical quarter range.

Do not execute APPLY.